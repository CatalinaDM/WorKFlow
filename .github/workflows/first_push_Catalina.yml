name: Fisrt_push
on:
  push: 

jobs:
  Tarea1:
    runs-on: ubuntu-latest

    steps:
      - name: Informe
        run: |
          echo "My first Push"
          echo "My name is Catalina"
          echo "Autor ${{github.event.head_commit.author.name}}"
          echo "Mensaje ${{github.event.head_commit.message}}"
